# 🎭 Playwright Test Scaffold

> AI 驱动的 Playwright 自动化测试脚手架 - 用自然语言生成测试

## ✨ 特性

- 🤖 **AI 对话生成** - 直接用自然语言描述需求，AI 自动生成测试
- 🔍 **自动页面分析** - 自动识别页面类型、元素、表单结构
- ⚡ **代码生成** - 自动生成 Page Object 和测试用例代码
- 🔐 **智能登录处理** - 自动分析并实现登录流程
- 📊 **Allure 报告** - 美观的可视化测试报告

## 🚀 快速开始

### 1. 环境准备

```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt
playwright install chromium
```

### 2. 配置项目

编辑 `config/project.yaml` 设置你的项目 URL：

```yaml
environments:
  default: "dev"
  dev:
    base_url: "https://localhost:3000"
```

### 3. 与 AI 对话生成测试

在 Cursor 中直接对话：

```
用户: 测试数据@test_account_pool.json，
      帮我生成 https://localhost:3000/admin/profile 页面的测试报告，
      使用 4 个 worker 执行
```

AI 将自动完成：
1. 🔍 **分析页面结构** - 打开目标 URL，识别页面元素
2. 📝 **生成 Page Object** - 创建 `pages/xxx_page.py`
3. ⚡ **生成测试用例** - 创建 `tests/test_xxx.py`
4. 🔐 **处理登录流程** - 如需登录，自动实现登录逻辑
5. 🚀 **执行测试** - 运行 pytest 并行测试
6. 📊 **生成报告** - 生成并打开 Allure 报告

## 📖 对话指令示例

| 指令类型 | 示例 |
|----------|------|
| **生成测试** | "帮我生成 https://example.com/login 页面的测试" |
| **指定测试数据** | "测试数据@test_account_pool.json，生成测试" |
| **指定并行数** | "使用 4 个 worker 执行测试" |
| **只运行核心测试** | "只运行 P0 级别的测试用例" |
| **调试模式** | "用 headed 模式运行测试" |
| **修复问题** | "测试失败了，帮我修复" |
| **查看报告** | "打开 Allure 报告" |

## 📁 项目结构

```
playwright-test-scaffold/
├── core/                     # 核心框架
│   ├── base_page.py         # 页面基类
│   ├── page_utils.py        # 页面工具
│   └── fixtures.py          # pytest fixtures
├── generators/               # 代码生成器
│   ├── page_analyzer.py     # 页面分析器
│   └── test_code_generator.py   # 代码生成
├── pages/                    # Page Objects
├── tests/                    # 测试用例
├── test-data/               # 测试数据
├── config/                   # 配置文件
├── allure-results/          # Allure 数据
└── allure-report/           # 测试报告
```

## 🏷️ 测试优先级

| 标记 | 说明 |
|------|------|
| `P0` | 核心功能测试（必须通过） |
| `P1` | 重要功能测试 |
| `P2` | 一般功能测试 |

## 🔐 测试账号

测试账号位于 `test-data/test_account_pool.json`，AI 会自动识别和使用。

## 📊 查看报告

对话中直接说：
```
用户: 打开 Allure 报告
```

或手动执行：
```bash
allure serve allure-results
```

---

**Happy Testing! 🎭**
